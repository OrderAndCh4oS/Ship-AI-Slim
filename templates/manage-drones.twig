{% extends 'base.twig' %}

{% block title_tag %}
    {{ parent() }} Manage Drones
{% endblock %}

{% block content %}
    <h1 class="title cyan">Squadron name: {{ drones[0].squadron.name }}</h1>
    <table>
        <tr>
            <th>Name</th>
            <th>Thruster Power</th>
            <th>Turning Speed</th>
        </tr>
        {% for drone in drones %}
            <tr>
                <td>{{ drone.name }}</td>
                <td>{{ drone.thrusterPower }}</td>
                <td>{{ drone.turningSpeed }}</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}